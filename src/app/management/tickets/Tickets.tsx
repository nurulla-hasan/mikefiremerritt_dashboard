import PageLayout from "@/components/common/page-layout";

const Tickets = () => {
  return (
    <PageLayout pagination={null}>
      <h1 className="text-xl font-semibold mb-4">Manage Ticket</h1>
    </PageLayout>
  );
};

export default Tickets;
